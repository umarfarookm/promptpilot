services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: promptpilot
      POSTGRES_USER: promptpilot
      POSTGRES_PASSWORD: promptpilot
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
